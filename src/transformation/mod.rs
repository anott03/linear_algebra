mod spec;

#[allow(dead_code)]
pub mod transformation {
    use std::ops;

    // v: vector to be rotated
    // angle: angle to rotate in degrees; positive is counterclockwise
    pub fn rotation(v: &Vec<i32>, angle: i32) -> Result<Vec<i32>, String> {
        return Ok(Vec::new());
    }
}
